{
  "name": "balanca-tournament-frontend",
  "version": "1.0.0",
  "description": "Frontend for Balanca Tournament",
  "scripts": {
    "serve": "npx serve . -p 3000 --cors",
    "dev": "npx live-server --port=3000 --open=pages/setup.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.0"
  }
}
